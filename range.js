/**
const Range = function (start, stop, step) {
    return Array.from({ length: (stop - start) / step + 1 }, (_, i) => start + (i * step))

}
const RangeNamed = ({ start = 0, stop = 30, step = 3 } = {}) => { return Array.from({ length: (stop - start) / step + 1 }, (_, i) => start + (i * step)) }

let = start = 1, stop = 20, step = 2;
console.log(RangeNamed({ start: 10, stop, step }))
**/
